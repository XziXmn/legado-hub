fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
# extras are load-bearing: http2 keeps site-fetches on HTTP/2 (h2 used to arrive
# transitively via primp 1.x and its disappearance in primp 2.0 broke fetching);
# socks keeps proxy support, brotli keeps br content-encoding.
httpx[http2,brotli,socks]>=0.27.0
lxml>=5.0.0
cssselect>=1.2.0
beautifulsoup4>=4.12.0
pyyaml>=6.0
pycryptodome>=3.20.0
curl_cffi>=0.15.0
ddgs>=9.0.0
opencc-python-reimplemented>=0.1.7
playwright>=1.44.0
requests>=2.31.0
